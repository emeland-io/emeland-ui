apiVersion: v1
kind: Service
metadata:
  name: {{ include "emeland-ui.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "emeland-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "emeland-ui.selectorLabels" . | nindent 4 }}
